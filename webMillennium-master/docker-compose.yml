version: '3.8'

services:
  webmillennium:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webmillennium-app
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    restart: unless-stopped
